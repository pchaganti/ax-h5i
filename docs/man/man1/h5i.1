.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.TH h5i 1  "h5i 0.4.1" 
.SH NAME
h5i \- Disposable, confined development environments for coding agents
.SH SYNOPSIS
\fBh5i\fR [\fB\-h\fR|\fB\-\-help\fR] [\fB\-V\fR|\fB\-\-version\fR] <\fIsubcommands\fR>
.SH DESCRIPTION
Disposable, confined development environments for coding agents
.SH OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
\fB\-V\fR, \fB\-\-version\fR
Print version
.SH SUBCOMMANDS
.TP
h5i\-box(1)
Disposable, confined development boxes. `h5i box` with no verb creates one from the current repository; `h5i box \-\-help` has the verb table
.TP
h5i\-ui(1)
Open the box console in a browser: one read\-only screen over the whole fleet. What each box is, what its policy actually allows, what ran inside it, and what pressed on a boundary
.TP
h5i\-browser(1)
Browser sessions: open one, drive it, close it
.TP
h5i\-join(1)
Open a box someone else is sharing, from a ticket they sent you
.TP
h5i\-runner(1)
Run boxes on another Linux machine you own
.TP
h5i\-plugin(1)
Install, list or remove a plugin
.TP
h5i\-skill(1)
Write or print the agent skill this binary carries
.TP
h5i\-completion(1)
Generate a shell completion script (bash, zsh, fish, ...); e.g. `h5i completion bash > /etc/bash_completion.d/h5i`
.TP
h5i\-help(1)
Print this message or the help of the given subcommand(s)
.SH VERSION
v0.4.1
.SH "H5I BOX"
Disposable, confined development boxes. `h5i box` with no verb creates one from the current repository; `h5i box --help` has the verb table
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBbox\fR [\fB\-\-pr\fR] [\fB\-\-new\fR] [\fB\-\-name\fR] [\fB\-\-from\fR] [\fB\-\-remote\fR] [\fB\-\-profile\fR] [\fB\-\-isolation\fR] [\fB\-\-image\fR] [\fB\-\-engine\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] [\fISOURCE\fR] [\fIsubcommands\fR]
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-pr\fR \fI<NUMBER|URL>\fR
Base the box on a GitHub pull request (number, #number, or URL)
.TP
\fB\-\-new\fR
Start from an empty box instead of any source
.TP
\fB\-\-name\fR \fI<NAME>\fR
Name for the box. Derived from the source when omitted
.TP
\fB\-\-from\fR \fI<FROM>\fR
Base revision, when the source is this repository. Pinned immutably.

Same conflicts as the explicit `box create` form. Without them the short form parsed `\-\-new \-\-from <rev>` and `\-\-pr N \-\-from <rev>` happily and then discarded the base: `into_command()` builds `Create` *after* clap has validated, so the explicit form\*(Aqs rules never applied. A silently unpinned base is an integrity gap in a tool whose pitch is that the base is pinned immutably.
.TP
\fB\-\-remote\fR \fI<REMOTE>\fR [default: origin]
Remote to fetch a pull request head from
.TP
\fB\-\-profile\fR \fI<PROFILE>\fR
Policy profile from .h5i/env.toml (see `h5i box create \-\-help`)
.TP
\fB\-\-isolation\fR \fI<ISOLATION>\fR
Isolation tier: auto (default) | workspace | process | supervised | container | ...
.TP
\fB\-\-image\fR \fI<IMAGE>\fR
Container base image for isolation=container
.TP
\fB\-\-engine\fR \fI<ENGINE>\fR
Browser engine for the `browser` profile: chromium | lightpanda | h5i
.TP
\fB\-\-json\fR
Emit the created box\*(Aqs manifest as JSON instead of the human summary
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
[\fISOURCE\fR]
Where the code comes from: `.` for this repository (the default), or a repository URL to copy into the box. A pull request is `\-\-pr`
.SH "H5I BOX CREATE"
Create a box explicitly. `h5i box [SOURCE]` is the short form and takes the same flags; this is what it dispatches to
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBcreate\fR [\fB\-\-from\fR] [\fB\-\-pr\fR] [\fB\-\-clone\fR] [\fB\-\-new\fR] [\fB\-\-remote\fR] [\fB\-\-profile\fR] [\fB\-\-isolation\fR] [\fB\-\-image\fR] [\fB\-\-engine\fR] [\fB\-\-runner\fR] [\fB\-\-backend\fR] [\fB\-\-audit\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-from\fR \fI<FROM>\fR
Base revision (default: HEAD). Pinned immutably
.TP
\fB\-\-pr\fR \fI<NUMBER|URL>\fR
Base the env on a GitHub pull request (number, #number, or URL): fetches refs/pull/<n>/head from the remote, pins it as the immutable base, and points the env\*(Aqs parent branch at a local `pr/<n>` tracking branch, so propose/apply review the PR head, and apply prints the push\-back command. Needs only `git`; `gh` (optional) enriches the push\-back hint with the PR\*(Aqs head branch name
.TP
\fB\-\-clone\fR \fI<URL>\fR
Copy an external repository into the box instead of taking a worktree of this one. The box is *detached*: this repository is never touched, and `h5i box export` is the only way out
.TP
\fB\-\-new\fR
Start from an empty box (a fresh repository with one empty commit), for letting an agent build something from nothing. Also detached
.TP
\fB\-\-remote\fR \fI<REMOTE>\fR [default: origin]
Remote to fetch the PR head from (with \-\-pr)
.TP
\fB\-\-profile\fR \fI<PROFILE>\fR
Policy profile from .h5i/env.toml. Built\-ins need no file: `agent` (agent\-in\-box, scoped to $H5I_AGENT\*(Aqs runtime), `agent\-claude` / `agent\-codex` (pin one runtime: only that agent\*(Aqs HOME state + API egress), `browser` (the agent profile plus headless Chrome and the agent\-browser daemon, so the agent can drive a real browser against the dev server it just started), and `default` (fail\-closed build/test confinement). Unset auto\-picks the creating runtime\*(Aqs agent profile when this host can enforce it, else `default`
.TP
\fB\-\-isolation\fR \fI<ISOLATION>\fR
Isolation: auto (default) | workspace | process | supervised | container | microvm. `auto` (or unset) picks the strongest tier the host can run; an explicit tier fails closed if the host cannot satisfy it (never silently downgrades). `microvm` boots a hardware\-isolated guest via microsandbox (`msb`) and is the only tier whose net.egress allowlist is enforced by address rather than by an HTTP proxy; it needs an image and host virtualization
.TP
\fB\-\-image\fR \fI<IMAGE>\fR
Base image for isolation=container and isolation=microvm (pre\-pulled; runs never pull). Overrides the profile\*(Aqs `container.image` and the repo\-level `[container] image` default in .h5i/env.toml. Passing it makes both image\-backed tiers candidates for the isolation auto\-pick
.TP
\fB\-\-engine\fR \fI<ENGINE>\fR
Browser engine for the `browser` profile: chromium (default), lightpanda, or h5i. Overrides the profile\*(Aqs `engine`. Pinned in the policy digest, and never falls back: a box whose engine cannot serve a page fails and names the recreate, because switching engine changes what a page is able to do
.TP
\fB\-\-runner\fR \fI<NAME>\fR
Run this box on a paired runner instead of this machine
.TP
\fB\-\-backend\fR \fI<BACKEND>\fR [default: auto]
Workspace backend (auto|worktree)
.TP
\fB\-\-audit\fR \fI<AUDIT>\fR [default: signal]
Audit capture mode for wrapped in\-env commands: signal (default) | all. `all` records every wrapped command, including small successful output
.TP
\fB\-\-json\fR
Emit the created box\*(Aqs manifest as JSON (the `status \-\-json` shape, plus the workspace path) instead of the human summary. Notes still go to stderr, so stdout stays parseable
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fINAME\fR>
Environment name (lowercase slug, e.g. `fix\-auth`)
.SH "H5I BOX RUN"
Run one command inside a box, policy-enforced: the exit code passes through and the run is recorded as a receipt tagged with the box id and the policy digest that was actually enforced
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBrun\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> [\fICOMMAND\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR
Emit a JSON envelope on stdout (receipt, exit code, timing, the recorded output) instead of streaming the raw output. Put the flag before the name: everything after `\-\-` belongs to the command. The exit code still passes through
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>
Environment name (slug, `agent/slug`, or full `env/agent/slug`)
.TP
[\fICOMMAND\fR]
The command to run, after `\-\-`
.SH "H5I BOX SHELL"
Open an interactive, confined session INSIDE the environment. The "agent-in-box". stdio is inherited (a real terminal), so every command the session spawns is contained by the box, not by the agent choosing to wrap each call. Defaults to a login shell when no command is given
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBshell\fR [\fB\-\-readonly\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> [\fICOMMAND\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-readonly\fR
Attach as a READ\-ONLY observer: `$WORK` is pinned read\-only, the box gets an ephemeral per\-session HOME/tmp, and no env state (status, captures, manifest) is touched. Multiple `\-\-readonly` sessions can run on one env at once (they take a shared lock); they still exclude a live read\-write session. Requires isolation=process or supervised
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>
Environment name (slug, `agent/slug`, or full `env/agent/slug`)
.TP
[\fICOMMAND\fR]
Command to run inside the box (after `\-\-`); default: an interactive shell
.SH "H5I BOX ALLOW"
Manage the persistent user-level egress allowlist: extra hosts merged into every container- or microvm-tier env whose profile already sets net.egress (a deny-all profile is never widened). Stored host-side under ~/.config/h5i/, outside every box-granted path; takes effect at the next `env run`/`env shell`. With no rule, lists the current entries
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBallow\fR [\fB\-\-remove\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] [\fIRULE\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-remove\fR
Remove the rule instead of adding it
.TP
\fB\-\-json\fR
Emit the current allowlist as JSON
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
[\fIRULE\fR]
Host rule: exact `api.example.com`, wildcard `.example.com` / `*.example.com`, optionally with a `:port` suffix
.SH "H5I BOX PROBE"
Probe what isolation this host can actually provide (Landlock, user namespaces, seccomp) and which claims are satisfiable
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBprobe\fR [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BOX DETECT"
Runtime detection: what the kernel saw inside a box
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBdetect\fR [\fB\-h\fR|\fB\-\-help\fR] <\fIsubcommands\fR>
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.SH "H5I BOX DETECT PROBE"
What this host can do for runtime detection, and what to run if it cannot
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBprobe\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BOX DETECT RULES"
The signature catalogue: every rule, its severity, and what it is for
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBrules\fR [\fB\-\-filter\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-filter\fR \fI<FAMILY|ID>\fR
Only this family (`net`, `secret`, `exec`, `priv`, `kernel`, `mount`) or this exact rule id
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BOX DETECT SHOW"
What the kernel observed in a box, across all of its receipts
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBshow\fR [\fB\-\-min\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-min\fR \fI<MIN>\fR [default: info]
Only detections at this severity or above (`info`, `notice`, `alert`)
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>
Box name or id
.SH "H5I BOX CAPABILITIES"
Machine-readable host enforcement report: isolation tier, egress-enforced yes/no, resource-limit support, and per-claim satisfiable/runnable, so a product can adapt to the real host without scraping `env probe` text
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBcapabilities\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR
Emit the structured report as JSON instead of the human view
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BOX LIST"
List environments on this clone (the fleet view)
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBlist\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR
Emit a machine\-readable JSON array (manifest + base drift per env)
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BOX STATUS"
Show one environment's status: lifecycle, enforced policy, evidence, and base drift
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBstatus\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR
Emit the raw manifest JSON instead of the human view
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I BOX VIEW"
Watch this box's browser from the host, and take over when you want to
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBview\fR [\fB\-\-port\fR] [\fB\-\-term\fR] [\fB\-\-fps\fR] [\fB\-\-assume\-graphics\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-port\fR \fI<PORT>\fR [default: 7331]
Loopback port to bind. 0 picks a free one and prints it
.TP
\fB\-\-term\fR
Draw the page in this terminal instead of serving it to a browser
.TP
\fB\-\-fps\fR \fI<FPS>\fR [default: 10]
Frame\-rate ceiling asked of the box, for `\-\-term`
.TP
\fB\-\-assume\-graphics\fR
Skip the graphics probe and render anyway, for `\-\-term`. Frames are then sent uncompressed (about six times the bytes), because the probe is also what asks whether this terminal accepts deflated ones
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I BOX SHARE"
Let one other person try this box's web app, from their own machine
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBshare\fR [\fB\-\-port\fR] [\fB\-\-expire\fR] [\fB\-\-label\fR] [\fB\-\-tunnel\fR] [\fB\-\-direct\-only\fR] [\fB\-h\fR|\fB\-\-help\fR] [\fINAME\fR] [\fIsubcommands\fR]
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-port\fR \fI<PORT>\fR [default: 3000]
The port inside the box. Whatever the dev server in there binds.

Not `0`: that is not a port a server can be listening on, and the same spelling means "pick a free one" on `h5i join`, which is the opposite.
.TP
\fB\-\-expire\fR \fI<EXPIRE>\fR [default: 60m]
How long the ticket lasts: `30m`, `2h`, `90s`, or bare seconds. 24h max
.TP
\fB\-\-label\fR \fI<LABEL>\fR
A name for the peer you are sending this to, for `status` and the receipt
.TP
\fB\-\-tunnel\fR
Publish through a Cloudflare quick tunnel instead of peer to peer, so the other side needs no h5i. Just a browser.

The trade is real and is recorded in the receipt: Cloudflare terminates TLS, so this path is not end\-to\-end encrypted.
.TP
\fB\-\-direct\-only\fR
Refuse to move any application byte over a relay. If a direct peer\-to\-peer path cannot be established, the share fails instead
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
[\fINAME\fR]
The box whose dev server you want someone else to try
.SH "H5I BOX SHARE STATUS"
Show a share: its endpoint, its grants, and how long each has left
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBstatus\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR
Emit the share record as JSON instead of the human view
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I BOX SHARE LIST"
List this clone's share records, live ones and any left by a crash
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBlist\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BOX SHARE GRANT"
Mint another ticket for a running share, so a second person can join
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBgrant\fR [\fB\-\-label\fR] [\fB\-\-expire\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-label\fR \fI<LABEL>\fR
A name for this peer, to make `status` and the receipt readable
.TP
\fB\-\-expire\fR \fI<EXPIRE>\fR [default: 60m]
How long this ticket lasts: `30m`, `2h`, `90s`, or bare seconds. 24h max
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fINAME\fR>

.SH "H5I BOX SHARE REVOKE"
Revoke one peer's ticket. The share keeps serving everyone else, and any connection that peer already has is dropped within a second
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBrevoke\fR [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> <\fIGRANT\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.TP
<\fIGRANT\fR>

.SH "H5I BOX SHARE STOP"
Stop a share running in another terminal
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBstop\fR [\fB\-\-force\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-force\fR
Delete the share record instead of asking its process to stop.

For a record whose pid has been taken over by an unrelated process: h5i then believes the share is serving forever, and no other verb gets out of that. No receipt is written, because the process that would have written one is gone.
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fINAME\fR>

.SH "H5I BOX DOCTOR"
Check one environment's enforcement readiness and structural health (can it actually enforce its isolation claim here? are its refs intact?)
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBdoctor\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR
Emit the structured report as JSON instead of the human view
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I BOX SECRETS"
List the secret grants an environment's policy declares, with a dry-run resolution status (never the value: only a fingerprint when resolvable)
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBsecrets\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR
Emit the status as JSON instead of the human view
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I BOX SERVICE"
Manage long-lived services declared in the env's `.h5i/env.toml` (`[service.<name>]`), confined and pid-tracked, no daemon
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBservice\fR [\fB\-h\fR|\fB\-\-help\fR] <\fIsubcommands\fR>
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BOX SERVICE START"
Start a declared service as a confined background process
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBstart\fR [\fB\-h\fR|\fB\-\-help\fR] <\fIENV\fR> <\fISERVICE\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fIENV\fR>
Environment name (slug, `agent/slug`, or full `env/agent/slug`)
.TP
<\fISERVICE\fR>
Service name (a `[service.<name>]` in the env\*(Aqs `.h5i/env.toml`)
.SH "H5I BOX SERVICE STOP"
Stop a running service (and capture its log as evidence)
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBstop\fR [\fB\-h\fR|\fB\-\-help\fR] <\fIENV\fR> <\fISERVICE\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fIENV\fR>

.TP
<\fISERVICE\fR>

.SH "H5I BOX SERVICE STATUS"
Show every recorded service for an env and its liveness
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBstatus\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fIENV\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fIENV\fR>

.SH "H5I BOX SERVICE LOGS"
Tail a running service's log
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBlogs\fR [\fB\-\-tail\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fIENV\fR> <\fISERVICE\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-tail\fR \fI<TAIL>\fR [default: 200]

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fIENV\fR>

.TP
<\fISERVICE\fR>

.SH "H5I BOX PORTS"
Show the per-env dynamic port map (services with a declared port)
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBports\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR
Emit the port map as JSON instead of the human view
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I BOX REBASE"
Rebase the environment onto its parent branch's current tip, re-pinning the base (use when `status` reports the parent has advanced)
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBrebase\fR [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I BOX LOG"
Show the event log (refs/h5i/env) for one environment
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBlog\fR [\fB\-\-limit\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-limit\fR \fI<LIMIT>\fR [default: 0]
Show only the newest N events (0 shows the full log)
.TP
\fB\-\-json\fR
Emit the event records as JSON instead of the human view
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I BOX WATCH"
Stream this box's policy decisions, one line each, as they happen
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBwatch\fR [\fB\-\-deny\-only\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-deny\-only\fR
Print only refusals: a denied request, the verdict that refused it, and any agent action the mediator turned away
.TP
\fB\-\-json\fR
Emit one JSON object per line instead of the human view
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fINAME\fR>

.SH "H5I BOX DIFF"
Diff the environment's work against its pinned base
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBdiff\fR [\fB\-\-stat\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-stat\fR
Show a diffstat instead of the full patch
.TP
\fB\-\-json\fR
Output a machine\-readable diffstat
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I BOX INSPECT"
Inspect one of an environment's evidence captures (structured findings, exit code, policy digest, redactions)
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBinspect\fR <\fB\-\-capture\fR> [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-capture\fR \fI<CAPTURE>\fR
Capture id (from `h5i box status`/`log`)
.TP
\fB\-\-json\fR
Emit the stored capture manifest as JSON instead of the human view
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I BOX COMPARE"
Compare environments side by side. Changes + latest run results (the "arena" reviewer comparison). Best on envs sharing one base
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBcompare\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAMES\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR
Emit JSON instead of the table
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAMES\fR>
Two or more environment names
.SH "H5I BOX EXPORT"
The output gate: freeze the box and write an inspectable bundle (patch.diff, report.md, receipt.json) for a human to review and apply. Nothing reaches the host repository without that human step
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBexport\fR [\fB\-\-out\fR] [\fB\-\-force\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-out\fR \fI<OUT>\fR
Where to write the bundle (default: ./h5i\-export/<name>)
.TP
\fB\-\-force\fR
Replace a non\-empty output directory
.TP
\fB\-\-json\fR
Emit the export summary as JSON instead of the human view
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I BOX PROPOSE"
Snapshot the worktree (mediated commit, path-allowlist enforced) and mark the box proposed. Produces a review brief. Never writes the parent branch. `export` runs this as its freeze step
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBpropose\fR [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I BOX APPLY"
Apply a proposed environment onto its parent branch (reviewer-selected, never automatic). Default is a merge (fast-forward when possible)
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBapply\fR [\fB\-\-patch\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-patch\fR
Squash the env\*(Aqs changes into a single commit instead of merging
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I BOX ABORT"
Abort an environment. Manifest and workspace preserved for forensics
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBabort\fR [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I BOX CACHE"
Warm dependency caches: one per project and ecosystem, keyed by that ecosystem's lockfiles, mounted read-only into a box
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBcache\fR [\fB\-h\fR|\fB\-\-help\fR] <\fIsubcommands\fR>
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BOX CACHE LIST"
List the caches on this clone
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBlist\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BOX CACHE MOUNTS"
Show which caches a box created right now would be given, and where they would appear inside it
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBmounts\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BOX CACHE REFRESH"
Populate a cache by fetching this project's dependencies
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBrefresh\fR [\fB\-h\fR|\fB\-\-help\fR] <\fIECO\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fIECO\fR>
Ecosystem to refresh (cargo, npm, uv, go)
.SH "H5I BOX CACHE RM"
Remove a cache. Without --key, removes every cache for the ecosystem
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBrm\fR [\fB\-\-key\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fIECO\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-key\fR \fI<KEY>\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fIECO\fR>

.SH "H5I BOX RM"
Permanently remove one or more environments from this clone: prune their worktrees, delete their code + reasoning branches, and erase their manifests. Destroys local provenance (only the `removed` event in refs/h5i/env survives). A still-live env (created/running/proposed) needs --force. Multiple names are processed in order; a failure for one env is reported but does not abort the rest
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBrm\fR [\fB\-\-force\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAMES\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-force\fR
Remove even if the env is still live (not applied/aborted). Applies to every listed env
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAMES\fR>
One or more environment names (slug, `agent/slug`, or full `env/agent/slug`)
.SH "H5I BOX GC"
Reclaim workspaces of applied/aborted environments (worktree prune). Manifests, branches, and captures are retained
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBgc\fR [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I UI"
Open the box console in a browser: one read-only screen over the whole fleet. What each box is, what its policy actually allows, what ran inside it, and what pressed on a boundary
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBui\fR [\fB\-\-port\fR] [\fB\-\-open\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-port\fR \fI<PORT>\fR [default: 8765]
Port to bind on 127.0.0.1. `0` asks the OS for a free one
.TP
\fB\-\-open\fR
Also hand the URL to this desktop\*(Aqs browser
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.SH "H5I BROWSER"
Browser sessions: open one, drive it, close it
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBbrowser\fR [\fB\-h\fR|\fB\-\-help\fR] <\fIsubcommands\fR>
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BROWSER OPEN"
Open a URL, making a session if there is not one already
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBopen\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-new\fR] [\fB\-\-in\fR] [\fB\-\-allow\fR] [\fB\-\-no\-loopback\fR] [\fB\-\-script\fR] [\fB\-\-permissive\-cors\fR] [\fB\-\-no\-sandbox\fR] [\fB\-\-secret\fR] [\fB\-\-identity\fR] [\fB\-\-width\fR] [\fB\-\-height\fR] [\fB\-\-expires\-in\fR] [\fB\-\-restore\fR] [\fB\-\-capture\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fIURL\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Name this session, so several can run at once.

A name is not an identity: it can be reused once the session it named has ended. The opaque id in `\-\-json` and in receipts is what cannot be, and is what to keep when you need a durable reference.
.TP
\fB\-\-new\fR
Make a new session even if one is already open.

Without this, `open` navigates the session it finds, because that is what opening a URL means when a browser is already up.
.TP
\fB\-\-in\fR \fI<BOX>\fR
Run the session inside this box instead of on this machine
.TP
\fB\-\-allow\fR \fI<ORIGIN>\fR
Grant an origin. Repeatable. Without any, nothing remote is reachable except the URL\*(Aqs own origin
.TP
\fB\-\-no\-loopback\fR
Refuse loopback too. It is reachable by default: it is the dev server
.TP
\fB\-\-script\fR
Run the page\*(Aqs own JavaScript. Off by default: with script off, page\-borne prompt injection has no delivery channel at all
.TP
\fB\-\-permissive\-cors\fR
Let this session\*(Aqs pages send credentials cross\-origin as a browser does: `mode: "no\-cors"` with `credentials: "include"`.

Refused by default, which is right for containing an agent: an opaque response cannot be read, so nothing can check the server agreed. It is also the classic POST\-CSRF vector, so the refusal stopped h5i acting as the victim in a CSRF test. Fixed at creation, in the policy digest, and named in `h5i browser status`.
.TP
\fB\-\-no\-sandbox\fR
Run the engine unconfined.

By default a session on this machine runs in a process\-tier sandbox: it may read the system and its own directory, write only its own directory, execute nothing, and see only the secrets named with `\-\-secret`. That contains what a parser bug could *do*; it does not contain the network, which needs a boundary outside the engine.
.TP
\fB\-\-secret\fR \fI<NAME>\fR
Let this session substitute `$H5I_SECRET_<NAME>`
.TP
\fB\-\-identity\fR \fI<NAME|PATH>\fR [default: native]
Who this session says it is: a built\-in name, or a path to a TOML file
.TP
\fB\-\-width\fR \fI<WIDTH>\fR [default: 1280]
Viewport width
.TP
\fB\-\-height\fR \fI<HEIGHT>\fR [default: 720]
Viewport height
.TP
\fB\-\-expires\-in\fR \fI<SECONDS>\fR
End the session automatically after this many seconds.

Recorded as an ending on the session\*(Aqs record when it passes, not as a disappearance: `h5i browser status` still answers afterwards, and says it expired.
.TP
\fB\-\-restore\fR \fI<SESSION_ID>\fR
Seed this session\*(Aqs storage from one that has ended.

A restore is a new session with a new id, and the inheritance is written into its record. Nothing resurrects an id: an agent holding a stale one always gets a refusal, never a different session wearing the same name. Takes an id, not a name, because a name can be reused and storage has to come from one definite session.
.TP
\fB\-\-capture\fR
Keep every request and response this session makes: headers and bodies, both directions.

Off by default, and a different thing from the request log, which is always on. The log records decisions in a form that is safe to paste into a bug report; this records the messages, `Authorization` header and session cookie included, which is what an HTTP workbench needs and what an audit trail must not carry. Stored `0700` inside the session directory, bounded, and never copied by `\-\-restore`.
.TP
\fB\-\-json\fR
Print the session record as JSON instead of a summary line
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fIURL\fR>
A URL, or a path to a local HTML file
.SH "H5I BROWSER IDENTITY"
List, show or check a browser identity
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBidentity\fR [\fB\-h\fR|\fB\-\-help\fR] <\fIARGS\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fIARGS\fR>
`list`, `show <name>`, or `check <name>`
.SH "H5I BROWSER READ"
Read one page, or a batch of them, and leave no session behind
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBread\fR [\fB\-\-in\fR] [\fB\-\-text\fR] [\fB\-\-script\fR] [\fB\-\-no\-sandbox\fR] [\fB\-\-identity\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fITARGETS\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-in\fR \fI<BOX>\fR
Read inside this box
.TP
\fB\-\-text\fR
Print the page\*(Aqs prose instead of its outline
.TP
\fB\-\-script\fR
Run the page\*(Aqs own JavaScript
.TP
\fB\-\-no\-sandbox\fR
Read unconfined
.TP
\fB\-\-identity\fR \fI<NAME|PATH>\fR [default: native]
Who this read says it is: a built\-in name, or a path to a TOML file.

The same identities a session takes, and refused on the same terms: see `h5i browser identity`. It belongs here as much as on a session. A read is a fetch, and a fetch has an agent string.
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fITARGETS\fR>
URLs, or paths to local HTML files
.SH "H5I BROWSER LIST"
List the browser sessions on this machine
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBlist\fR [\fB\-\-all\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-all\fR
Include sessions that have ended. They are kept: the record of how a session ended is the part a reviewer needs
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BROWSER STATUS"
What a session is, where it runs, and who saw its network
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBstatus\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BROWSER CLOSE"
End a session. Its record stays
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBclose\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-all\fR] [\fB\-\-capture\-drop\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-all\fR
End every live session on this machine
.TP
\fB\-\-capture\-drop\fR
Delete the captured messages as well as ending the session.

The store holds whole bodies and `Cookie` and `Authorization` in full. The record and the request log stay either way.
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.SH "H5I BROWSER SNAPSHOT"
The page as a model should read it: the outline, with `@ref` handles
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBsnapshot\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-delta\fR] [\fB\-\-url\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-delta\fR
Report only what changed since the last snapshot
.TP
\fB\-\-url\fR \fI<URL>\fR
Go here first, then read. One round trip where `navigate` and then this would be two, and the reply still names the URL it ended up on so a redirect is not silent
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BROWSER NAVIGATE"
Go to a URL, resolved against the current page like a click would be
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBnavigate\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fIURL\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fIURL\fR>

.SH "H5I BROWSER SCREENSHOT"
Write a PNG of the page this session is on
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBscreenshot\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-out\fR] [\fB\-\-url\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-out\fR \fI<PATH>\fR
Where to write it, on *this* machine. Defaults to a host\-named file in the session\*(Aqs own artifacts directory.

The engine paints into a directory it is allowed to write and h5i moves the file here afterwards, so this is any path you can write, not a path the confined engine can. For a session in a box that means the file is carried out of the box, which needs a tier whose `/tmp` this machine shares, and says so when it does not.
.TP
\fB\-\-url\fR \fI<URL>\fR
Go here first, then paint
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.SH "H5I BROWSER SOCKET"
Open a WebSocket, send frames, and report what came back
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBsocket\fR [\fB\-\-send\fR] [\fB\-\-wait\-ms\fR] [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fIURL\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-send\fR \fI<TEXT>\fR
A text frame to send. Repeatable, and sent in the order given
.TP
\fB\-\-wait\-ms\fR \fI<MS>\fR [default: 5000]
How long to listen for replies, in milliseconds.

A server that answers by doing something first \(em running a command, say \(em answers late, and a socket that says nothing at all is a result rather than an error.
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fIURL\fR>
The `ws://` or `wss://` endpoint to open
.SH "H5I BROWSER RELOAD"
Fetch the current URL again
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBreload\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.SH "H5I BROWSER CLICK"
Follow a `@ref` from the last snapshot
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBclick\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-selector\fR] [\fB\-\-role\fR] [\fB\-\-name\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] [\fIREFERENCE\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-selector\fR \fI<CSS>\fR
A CSS selector instead, which survives a navigation where a `@ref` does not
.TP
\fB\-\-role\fR \fI<ROLE>\fR
Find it by what it is called, the way a person would say it: `\-\-role button \-\-name "Sign in"`
.TP
\fB\-\-name\fR \fI<TEXT>\fR

.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
[\fIREFERENCE\fR]
`e3` or `@e3`, from a `snapshot`. Omit when using `\-\-selector` or `\-\-role`
.SH "H5I BROWSER TYPE"
Put text into a field, replacing what was there
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBtype\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-selector\fR] [\fB\-\-role\fR] [\fB\-\-name\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] [\fIREF|TEXT\fR] [\fITEXT\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-selector\fR \fI<CSS>\fR
A CSS selector instead.

No conflict with the positional, unlike `click`\*(Aqs: with a locator given, the one positional left is the *text*, and the engine shifts it (`two_positionals`). Declaring a conflict here made `type \-\-selector \*(Aq#password\*(Aq hunter2` a usage error, which is the obvious way to type it.
.TP
\fB\-\-role\fR \fI<ROLE>\fR
Find the field by what it is called: `\-\-role textbox \-\-name Email`
.TP
\fB\-\-name\fR \fI<TEXT>\fR

.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
[\fIREF|TEXT\fR]
`e3` or `@e3`, from a `snapshot`. Omit when using `\-\-selector` or `\-\-role`, and give the text alone
.TP
[\fITEXT\fR]
What to type
.SH "H5I BROWSER SUBMIT"
Submit the form containing a `@ref`
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBsubmit\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-selector\fR] [\fB\-\-role\fR] [\fB\-\-name\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] [\fIREFERENCE\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-selector\fR \fI<CSS>\fR
A CSS selector instead
.TP
\fB\-\-role\fR \fI<ROLE>\fR
Find a control in the form by what it is called
.TP
\fB\-\-name\fR \fI<TEXT>\fR

.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
[\fIREFERENCE\fR]
`e3` or `@e3`, from a `snapshot`. Omit when using `\-\-selector` or `\-\-role`
.SH "H5I BROWSER SCROLL"
Scroll the page. Negative scrolls up
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBscroll\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fIBY\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fIBY\fR>

.SH "H5I BROWSER WAIT-FOR"
Wait until something is on the page, or until nothing can put it there
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBwait\-for\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-selector\fR] [\fB\-\-text\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-selector\fR \fI<CSS>\fR

.TP
\fB\-\-text\fR \fI<TEXT>\fR

.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BROWSER WAIT-FOR-SCRIPT"
Wait until a page expression is true. Needs `open --script`
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBwait\-for\-script\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fIEXPR\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fIEXPR\fR>

.SH "H5I BROWSER EXTRACT"
Pull structured data out of the page by selector
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBextract\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-url\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fISCHEMA\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-url\fR \fI<URL>\fR
Go here first, then read. One round trip where `navigate` and then this would be two, and the reply still names the URL it ended up on so a redirect is not silent
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fISCHEMA\fR>
The schema, as JSON
.SH "H5I BROWSER MARKDOWN"
The page as markdown: what a reader would read, without the handles
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBmarkdown\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-max\-bytes\fR] [\fB\-\-url\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-max\-bytes\fR \fI<BYTES>\fR

.TP
\fB\-\-url\fR \fI<URL>\fR
Go here first, then read. One round trip where `navigate` and then this would be two, and the reply still names the URL it ended up on so a redirect is not silent
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BROWSER AUDIT"
Everything recorded about this session, in one ordered timeline
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBaudit\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-no\-session\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-no\-session\fR
The helper runs that belong to no session.

`h5i browser transcript \-\-via yt\-dlp \-\-url <URL>` needs no page and so opens no session; the run is still recorded, and this is where. Kept out of a session\*(Aqs own timeline on purpose: a run that was not part of a session must not appear inside it.
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.SH "H5I BROWSER STRUCTURED"
What the page publishes *about itself*: JSON-LD, OpenGraph, `<meta>`, `<link rel>`
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBstructured\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-url\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-url\fR \fI<URL>\fR
Go here first, then read. One round trip where `navigate` and then this would be two, and the reply still names the URL it ended up on so a redirect is not silent
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.SH "H5I BROWSER TRANSCRIPT"
What the page's media *says*: `<track>` captions, fetched and parsed
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBtranscript\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-url\fR] [\fB\-\-lang\fR] [\fB\-\-max\-bytes\fR] [\fB\-\-via\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-url\fR \fI<URL>\fR
Go here first, then read. One round trip where `navigate` and then this would be two, and the reply still names the URL it ended up on so a redirect is not silent
.TP
\fB\-\-lang\fR \fI<LANG>\fR
Prefer this language, for the words and for the outline alike.

Prefix\-matched against `srclang`, so `en` finds `en\-GB`. Falls back to the track the page marked `default`, then to the first. Every track is listed either way.
.TP
\fB\-\-max\-bytes\fR \fI<BYTES>\fR
The ceiling on caption text carried out of *one* track.

Per track rather than per reply, so a media element with both words and an outline can carry up to twice this.
.TP
\fB\-\-via\fR \fI<HELPER>\fR
Read it with an outside program instead of from the page\*(Aqs markup
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.SH "H5I BROWSER SET-CHECKED"
Set a checkbox or radio to a state, rather than toggling it
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBset\-checked\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-selector\fR] [\fB\-\-role\fR] [\fB\-\-name\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] [\fIREF|STATE\fR] [\fISTATE\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-selector\fR \fI<CSS>\fR

.TP
\fB\-\-role\fR \fI<ROLE>\fR
Find the control by what it is called, the way a person would
.TP
\fB\-\-name\fR \fI<TEXT>\fR

.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
[\fIREF|STATE\fR]
`e3` or `@e3`, from a `snapshot`. Omit when using `\-\-selector` or `\-\-role`, and give the state alone
.TP
[\fISTATE\fR]
`true` or `false`
.SH "H5I BROWSER SELECT"
Choose an option in a `<select>`, by its value or by the text it shows
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBselect\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-selector\fR] [\fB\-\-role\fR] [\fB\-\-name\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] [\fIREF|OPTION\fR] [\fIOPTION\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-selector\fR \fI<CSS>\fR

.TP
\fB\-\-role\fR \fI<ROLE>\fR

.TP
\fB\-\-name\fR \fI<TEXT>\fR

.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
[\fIREF|OPTION\fR]
`e3` or `@e3`. Omit when using `\-\-selector` or `\-\-role`
.TP
[\fIOPTION\fR]
The option\*(Aqs value, or the text it shows, in that order
.SH "H5I BROWSER PRESS"
Press a key that *does* something: Enter, Escape, Tab, ArrowDown
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBpress\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-selector\fR] [\fB\-\-role\fR] [\fB\-\-name\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] [\fIREF|KEY\fR] [\fIKEY\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-selector\fR \fI<CSS>\fR

.TP
\fB\-\-role\fR \fI<ROLE>\fR

.TP
\fB\-\-name\fR \fI<TEXT>\fR

.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
[\fIREF|KEY\fR]
`e3` or `@e3`. Omit when using `\-\-selector` or `\-\-role`
.TP
[\fIKEY\fR]
The key name
.SH "H5I BROWSER FIND"
Find an element by what it is called, rather than by where it sits
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBfind\fR [\fB\-s\fR|\fB\-\-session\fR] <\fB\-\-role\fR> [\fB\-\-name\fR] [\fB\-\-url\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-role\fR \fI<ROLE>\fR
`button`, `link`, `textbox`, `checkbox`, ...
.TP
\fB\-\-name\fR \fI<TEXT>\fR
The accessible name, matched whole: case is ignored and whitespace collapsed, but half a name finds nothing
.TP
\fB\-\-url\fR \fI<URL>\fR
Go here first, then read. One round trip where `navigate` and then this would be two, and the reply still names the URL it ended up on so a redirect is not silent
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.SH "H5I BROWSER SCRIPT"
Record what this session did as a replayable script
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBscript\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-save\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-save\fR \fI<PATH>\fR
Write the script here instead of printing it
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.SH "H5I BROWSER REQUESTS"
The request log: what this session asked for, and what was refused
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBrequests\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-since\fR] [\fB\-\-method\fR] [\fB\-\-url\-contains\fR] [\fB\-\-status\fR] [\fB\-\-initiator\fR] [\fB\-\-denied\-only\fR] [\fB\-\-limit\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-since\fR \fI<SEQ>\fR
Only what happened after this sequence number
.TP
\fB\-\-method\fR \fI<METHOD>\fR
Only this method
.TP
\fB\-\-url\-contains\fR \fI<TEXT>\fR
Only rows whose URL contains this
.TP
\fB\-\-status\fR \fI<CODE>\fR
Only responses with this status
.TP
\fB\-\-initiator\fR \fI<KIND>\fR
Only `navigation`, `subresource`, `frame`, `redirect` or `replay`
.TP
\fB\-\-denied\-only\fR
Only what policy refused
.TP
\fB\-\-limit\fR \fI<N>\fR
At most this many rows, newest last
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.SH "H5I BROWSER SEQUENCE"
Run a multi-step flow: send, extract, send again with what was found
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBsequence\fR [\fB\-\-var\fR] [\fB\-\-keep\-going\fR] [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fIFILE\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-var\fR \fI<NAME=VALUE>\fR
Bind a name before the first step, as `name=value`. Repeatable
.TP
\fB\-\-keep\-going\fR
Run every step even after one fails, to read a whole file\*(Aqs failures at once
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fIFILE\fR>
The sequence file
.SH "H5I BROWSER SITEMAP"
What this session reached, as a tree of origins and endpoints
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBsitemap\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.SH "H5I BROWSER MESSAGE"
One stored message, as it went out or as it came back
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBmessage\fR [\fB\-\-part\fR] [\fB\-\-raw\fR] [\fB\-\-body\-to\fR] [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fISEQ\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-part\fR \fI<HALF>\fR
Which half. Both by default
.br

.br
\fIPossible values:\fR
.RS 14
.IP \(bu 2
request
.IP \(bu 2
response
.IP \(bu 2
both
.RE
.TP
\fB\-\-raw\fR
Print it as an HTTP message rather than a summary.

Wins over `\-\-json`: a wire message is bytes, and bytes wrapped in a JSON string are no longer the message.
.TP
\fB\-\-body\-to\fR \fI<PATH>\fR
Write the body to this file, exactly as it came back.

The store already holds the bytes; this is the way to get them out. A response is not always something to read \(em a database backup left in an open bucket, an image, an archive \(em and the next step is usually a tool that wants a file.

With `\-\-part both`, the response body if there is one, otherwise the request\*(Aqs.
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fISEQ\fR>
The sequence number, as `requests` lists them
.SH "H5I BROWSER DIFF"
How two of this session's responses differ
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBdiff\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fISEQ\fR> <\fISEQ\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fISEQ\fR>
The response to compare from
.TP
<\fISEQ\fR>
The response to compare to
.SH "H5I BROWSER MATCH"
Ask a stored response a question, for a script to branch on
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBmatch\fR [\fB\-\-regex\fR] [\fB\-\-contains\fR] [\fB\-\-json\-path\fR] [\fB\-\-header\fR] [\fB\-\-status\fR] [\fB\-\-longer\-than\fR] [\fB\-\-shorter\-than\fR] [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fISEQ\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-regex\fR \fI<PATTERN>\fR
A regular expression over the body. Capture groups come back
.TP
\fB\-\-contains\fR \fI<TEXT>\fR
A literal substring of the body. Needs no escaping, which matters when the thing being looked for is a payload
.TP
\fB\-\-json\-path\fR \fI<PATH[=VALUE]>\fR
A dotted path into a JSON body (`session.token`), or `path=value`
.TP
\fB\-\-header\fR \fI<NAME[=VALUE]>\fR
A header by name, or `name=value`
.TP
\fB\-\-status\fR \fI<CODE>\fR
The status code
.TP
\fB\-\-longer\-than\fR \fI<BYTES>\fR
The body is longer than this many bytes
.TP
\fB\-\-shorter\-than\fR \fI<BYTES>\fR
The body is shorter than this many bytes
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fISEQ\fR>
The response to ask about
.SH "H5I BROWSER RESEND"
Send one of this session's own requests again, with changes
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBresend\fR [\fB\-\-set\fR] [\fB\-\-set\-file\fR] [\fB\-\-unset\fR] [\fB\-\-create\fR] [\fB\-\-repeat\fR] [\fB\-\-race\fR] [\fB\-\-no\-follow\fR] [\fB\-\-reset\-budget\fR] [\fB\-\-as\fR] [\fB\-\-keep\-credentials\fR] [\fB\-\-raw\-target\fR] [\fB\-\-raw\-request\fR] [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fISEQ\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-set\fR \fI<TARGET=VALUE>\fR
Change one part of it: `query.id=456`, `header.X\-Real\-IP=127.0.0.1`, `cookie.session=forged`, `json.role=admin`, `form.user=admin`, `path=/admin`, `method=POST`, `body.raw=<bytes>`. Repeatable, and applied in the order given.

The value is everything after the first `=`, so a payload full of `=` needs no escaping.
.TP
\fB\-\-set\-file\fR \fI<TARGET=PATH>\fR
Set one part of it from a file: `multipart.userfile=./payload.jpg`.

The value is the file\*(Aqs bytes, unaltered. `\-\-set` cannot carry them: a command line is text and a JPEG begins `ff d8`, which is not text in any encoding. A magic\-number check is a filter worth testing, so there has to be a way to send a real one.

Applied after every `\-\-set`, in the order given.
.TP
\fB\-\-unset\fR \fI<TARGET>\fR
Remove one part of it, by the same names
.TP
\fB\-\-create\fR
Add a target that is not there rather than refusing.

Off by default: a parameter that does not exist is usually a typo, and a typo that silently succeeds costs a whole turn spent reading a response that was never going to differ.
.TP
\fB\-\-repeat\fR \fI<N>\fR [default: 1]
Send it this many times and report the clock, for a timing test.

Repeated inside the engine rather than by a shell loop: the thing being measured is milliseconds and starting a process costs tens of them, so a loop out here would measure the loop. The reply carries every sample plus a median and a median absolute deviation, which is the pair that survives one scheduling hiccup where a mean does not.
.TP
\fB\-\-race\fR
Release the repeated sends together, for a race.

The test for check\-then\-act: an application that reads a balance, decides, and writes it back has a window between the read and the write, and `\-\-repeat 20 \-\-race` is what finds out how wide it is.

A burst, and named as one: the sends leave from twenty threads that meet at a barrier first. It is not a single\-packet attack, which needs each request split across two writes with the last byte held back. Ordinary check\-then\-act windows do not need that.

Every send is a receipt and a stored message like any other, so a race that reproduces is a race somebody else can read afterwards.
.TP
\fB\-\-no\-follow\fR
Stop at the first redirect and report it, rather than following it.

A browser follows a `Location`; a test usually wants the 302 itself. It is where an authentication flow says who you are, where an open redirect proves it accepts anything, and where the `Set\-Cookie` that logs you in actually rides. The reply carries the status and the headers with nothing followed.
.TP
\fB\-\-reset\-budget\fR
Start this page\*(Aqs network allowance again before sending.

The budget exists to bound *page* code: a script in a loop is the untrusted thing it stops. A blind extraction is the opposite, an agent deliberately sending hundreds of requests it composed, and without this it stops partway through with "this page has spent its allowance" and every later answer reads as a negative result.

Grants nothing new: navigating resets the same allowance, so this is the same act without throwing away the page.
.TP
\fB\-\-as\fR \fI<SESSION>\fR
Send it from another session instead of this one.

The authorization test, in one flag: take the request one logged\-in session made and send it from a different logged\-in session. The message comes from `\-\-session`; the cookies, identity, policy and receipts are the named session\*(Aqs.

The source session\*(Aqs own credentials are dropped, because carrying them would send a request that is neither user\*(Aqs and whose answer means nothing. `\-\-keep\-credentials` sends them anyway, for the test where that is the question.
.TP
\fB\-\-keep\-credentials\fR
Carry the source session\*(Aqs `Cookie` and `Authorization` headers into the other session. Only meaningful with `\-\-as`
.TP
\fB\-\-raw\-target\fR \fI<TARGET>\fR
Send this request\-target unchanged, without URL normalization.

Policy, budget, and receipt checks still apply. The sender computes `Host` and `Content\-Length`; use `\-\-raw\-request` to control framing.
.TP
\fB\-\-raw\-request\fR \fI<PATH>\fR
Send a complete request unchanged from this file; `\-` reads stdin.

The stored URL supplies the authority for policy checks and dialing.
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fISEQ\fR>
The sequence number to send again, as `requests` lists them
.SH "H5I BROWSER ENV"
Which credentials this session can use, by name. Never their values
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBenv\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BROWSER LOGIN"
Hand the page to the human at the live view for as long as a login takes
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBlogin\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-off\fR] [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-off\fR
End login mode and make the page readable again
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BROWSER TAKE"
Take control as a human. The agent's automation pauses at its next verb
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBtake\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BROWSER RELEASE"
Hand control back to the agent. It must re-snapshot before acting, because the page moved while you were driving
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBrelease\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I BROWSER VIEW"
Watch this session's page, and take the controls
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBview\fR [\fB\-s\fR|\fB\-\-session\fR] [\fB\-\-web\fR] [\fB\-\-port\fR] [\fB\-\-fps\fR] [\fB\-\-assume\-graphics\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-s\fR, \fB\-\-session\fR \fI<NAME>\fR
Which session, when more than one is open. A name from `\-\-session` at open time, or an opaque id. Defaults to $H5I_BROWSER_SESSION, then to the session `open` last made
.TP
\fB\-\-web\fR
Serve the page to a browser over loopback instead of drawing it here.

For a terminal that cannot draw images, and for a page the keyboard cannot reach.
.TP
\fB\-\-port\fR \fI<PORT>\fR [default: 0]
Port for `\-\-web`. 0 picks a free one
.TP
\fB\-\-fps\fR \fI<N>\fR [default: 10]
Frame\-rate ceiling asked of the session
.TP
\fB\-\-assume\-graphics\fR
Skip the graphics probe and render anyway, for a terminal the probe gets wrong
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.SH "H5I BROWSER URL"
Print the loopback viewer URL for a box, token included. `h5i box view` is what actually serves it; this is for pasting into a browser when a forward is already running
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBurl\fR [\fB\-\-port\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-port\fR \fI<PORT>\fR [default: 7331]

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>
A box name
.SH "H5I JOIN"
Open a box someone else is sharing, from a ticket they sent you
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBjoin\fR [\fB\-\-port\fR] [\fB\-\-shared\-jar\fR] [\fB\-\-bind\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fITICKET\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-port\fR \fI<PORT>\fR [default: 0]
Local port to serve it on. 0 picks a free one and prints it
.TP
\fB\-\-shared\-jar\fR
Join even when the only address left is `127.0.0.1`
.TP
\fB\-\-bind\fR \fI<ADDR>\fR
Serve on this loopback address instead of a random `127.x.y.z`.

The address is bound exactly, no fallback, and only loopback (`127.0.0.0/8`) is accepted. This is the WSL answer: Windows forwards only `127.0.0.1` into the VM, so the private address a join normally picks binds fine and is then unreachable from a Windows browser. `\-\-bind 127.0.0.1` counts as shared\-jar consent by itself; any other loopback address keeps a cookie jar of its own.
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fITICKET\fR>
The `h5i1_...` ticket you were sent, or `\-` to read it from stdin.

`/proc/<pid>/cmdline` is world\-readable on an ordinary Linux box and this process runs for the whole session, so a ticket passed as an argument is legible to every other user on the machine for as long as you are joined, and a ticket is the whole authorization. `pbpaste | h5i join \-` keeps it out of the process table and your shell history.
.SH "H5I RUNNER"
Run boxes on another Linux machine you own
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBrunner\fR [\fB\-h\fR|\fB\-\-help\fR] <\fIsubcommands\fR>
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I RUNNER PAIR"
Pair with a Linux machine so boxes can run on it
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBpair\fR [\fB\-\-port\fR] [\fB\-\-fingerprint\fR] [\fB\-\-print\-only\fR] [\fB\-\-worker\-path\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> <\fI[USER@]HOST\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-port\fR \fI<PORT>\fR
SSH port, when it is not 22
.TP
\fB\-\-fingerprint\fR \fI<SHA256:...>\fR
Refuse to pair unless the host key matches this fingerprint.

Pairing otherwise trusts the key the machine presents the first time, exactly like your first `ssh` to a new host. Read the real one on the machine itself with `ssh\-keygen \-lf /etc/ssh/ssh_host_ed25519_key.pub` and pass it here to close that window.
.TP
\fB\-\-print\-only\fR
Print the `authorized_keys` line instead of installing it.

For a runner where you add keys by another route, or where you want to read the line before it goes in.
.TP
\fB\-\-worker\-path\fR \fI<PATH>\fR
Path to `h5i` on the runner, when it is not on the PATH of the account you are pairing with
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fINAME\fR>
A short name for this machine, used in commands and output. It is a label: the runner\*(Aqs identity is its host key, so renaming or re\-pointing a name never moves a box to different hardware
.TP
<\fI[USER@]HOST\fR>
Where it is: `user@host`, or just `host` to use your SSH default
.SH "H5I RUNNER PROBE"
Ask a runner what it can do right now
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBprobe\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fINAME\fR>

.SH "H5I RUNNER LIST"
List the machines this account has paired
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBlist\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I RUNNER BOXES"
List the boxes a runner is holding
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBboxes\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fINAME\fR>

.SH "H5I RUNNER DESTROY"
Remove a box from a runner
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBdestroy\fR [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> <\fIBOX_ID\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.TP
<\fIBOX_ID\fR>

.SH "H5I RUNNER GC"
Reap what the leases say is over
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBgc\fR [\fB\-\-all\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-all\fR
Reap every box, not only the expired ones
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fINAME\fR>

.SH "H5I RUNNER UNPAIR"
Forget a runner: its record, its key, and its pinned host key
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBunpair\fR [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fINAME\fR>

.SH "H5I PLUGIN"
Install, list or remove a plugin
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBplugin\fR [\fB\-h\fR|\fB\-\-help\fR] <\fIsubcommands\fR>
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.SH "H5I PLUGIN INSTALL"
Install a plugin from a file
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBinstall\fR <\fB\-\-from\fR> [\fB\-\-force\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-from\fR \fI<PATH>\fR
The executable to install
.TP
\fB\-\-force\fR
Replace one that is already installed
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fINAME\fR>
The plugin\*(Aqs name, as it will be typed: `websec` for `h5i websec`
.SH "H5I PLUGIN LIST"
What is installed, and what could be
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBlist\fR [\fB\-\-json\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-json\fR

.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I PLUGIN REMOVE"
Remove an installed plugin
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBremove\fR [\fB\-h\fR|\fB\-\-help\fR] <\fINAME\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fINAME\fR>

.SH "H5I SKILL"
Write or print the agent skill this binary carries
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBskill\fR [\fB\-h\fR|\fB\-\-help\fR] <\fIsubcommands\fR>
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I SKILL INSTALL"
Write the skill to disk. Defaults to the per-user skill directory for the current runtime; inside a box, bootstrap points `$H5I_SKILL_DIR` at the in-box location
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBinstall\fR [\fB\-\-target\fR] [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-\-target\fR \fI<TARGET>\fR
Write here instead of the default target
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I SKILL SHOW"
Print a page to stdout: the skill itself, or one reference (`boxes`, `policy`, `export`, `troubleshooting`)
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBshow\fR [\fB\-h\fR|\fB\-\-help\fR] [\fIPAGE\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
[\fIPAGE\fR]

.SH "H5I SKILL PATH"
Print where `install` would write
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBpath\fR [\fB\-h\fR|\fB\-\-help\fR] 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.SH "H5I COMPLETION"
Generate a shell completion script (bash, zsh, fish, ...); e.g. `h5i completion bash > /etc/bash_completion.d/h5i`
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS SYNOPSIS
\fBcompletion\fR [\fB\-h\fR|\fB\-\-help\fR] <\fISHELL\fR> 
.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.SS OPTIONS
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help
.TP
<\fISHELL\fR>
Shell to generate completions for
.br

.br
\fIPossible values:\fR
.RS 14
.IP \(bu 2
bash
.IP \(bu 2
elvish
.IP \(bu 2
fish
.IP \(bu 2
powershell
.IP \(bu 2
zsh
.RE
.SH SEE ALSO
Full narrative manual: \fBMANUAL.md\fR in the source tree, or the rendered \fB/manual/\fR page on the project site.
